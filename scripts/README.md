# VPN Checker Binary

Положи сюда скомпилированный бинарник `vpn_checker` (Linux) или `vpn_checker.exe` (Windows).

## Сборка

```bash
cd ../../vpn_checker_rust
cargo build --release
cp target/release/vpn_checker ../vpn_checker_bin/scripts/
```
